{
  "name": "agentgov",
  "version": "0.1.0",
  "description": "Trust and release governance for multi-agent Copilot Studio systems.",
  "type": "module",
  "bin": {
    "agentgov": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc && node dist/schema/exportJsonSchemas.js",
    "dev": "tsx src/server.ts",
    "cli": "tsx src/cli.ts",
    "test": "vitest run",
    "test:smoke": "npm run build && node dist/cli.js trust check fixtures/agent-cards/poisoned-injection.json --offline && node dist/cli.js release check target-agents/vendor-exception.yaml --eval fixtures/eval-results/block.json",
    "schema": "tsx src/schema/exportJsonSchemas.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "commander": "^12.1.0",
    "yaml": "^2.6.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "license": "MIT"
}
